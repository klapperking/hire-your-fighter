<%= simple_form_for([fighter, booking]) do |f| %>
  <%# <%= f.hidden_field :fighter_id, value: fighter.id %>
  <div class="row">
    <div class="col"><%= f.input :starting_date, as: :string, input_html: { data: { controller: "calendar" } } %></div>
    <div class="col"><%= f.input :expiration_date, as: :string, input_html: { data: { controller: "calendar" } }%></div>
  </div>
  <div class="d-grid"><%= f.submit "Book character!", class: "btn btn-ghost" %></div>
<% end %>

<%# <%= simple_form_for([fighter, booking]) do |f| %>
  <%# <%= f.hidden_field :fighter_id, value: fighter.id %>
  <%# <%= f.input :starting_date %>
  <%# <%= f.input :expiration_date %>
  <%# <%= f.submit %>
<%# <% end %>
